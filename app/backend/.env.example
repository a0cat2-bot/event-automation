PORT=3000
FRONTEND_ORIGIN=http://localhost:5173
DATABASE_URL=postgresql://app:app@localhost:5432/app_db

# Required only when using Sally browser-automation features.
SALLY_EMAIL=
SALLY_PASSWORD=
# Receives one alert when Sally survey creation no longer matches Sally's UI. Optional.
SALLY_AUTOMATION_ADMIN_EMAIL=

# Sally collects a bare Knox ID in question 1 and leaves its own Email column blank, but applicants
# are identified by email and letters are delivered there. Set the corporate mail domain so a Knox
# ID can be expanded into an address (e.g. samsung.com turns "gildong.hong" into
# "gildong.hong@samsung.com"). Leave blank and Sally rows whose Knox ID has no domain are flagged
# as errors rather than given a guessed address.
KNOX_EMAIL_DOMAIN=

# Recruitment recipient source. Knox mail/contacts access is not available yet, so keep this on
# manual and edit the per-program employee email list in the notice screen.
RECRUITMENT_RECIPIENT_SOURCE=manual

# Email sending. Switch to EMAIL_PROVIDER=knox_portal once KNOX_PORTAL_API_URL below is filled in.
EMAIL_PROVIDER=gmail
GMAIL_USER=
# Gmail App Password (requires Google Account 2FA), not the account login password.
GMAIL_APP_PASSWORD=
EMAIL_FROM_NAME=Program Team

# Knox Portal email provider (token/account based). Leave KNOX_PORTAL_API_URL blank until the
# internal office endpoint is available — selecting EMAIL_PROVIDER=knox_portal fails fast with a
# clear error until all three of these are set.
KNOX_PORTAL_API_URL=
KNOX_PORTAL_API_TOKEN=
KNOX_PORTAL_ACCOUNT=

# Access control. The application never stores or checks passwords — authentication is delegated
# to the corporate SSO / reverse proxy, and the app performs authorization (roles) only.
#   disabled   — no access control; every request is treated as an admin (previous behaviour,
#                kept as the default so existing deployments keep working). Logs a startup warning.
#   dev_header — identity from an unverified X-Dev-User-Email header. Local development only.
#   sso_header — identity from proxy-injected headers. ONLY safe when the backend is reachable
#                exclusively through the proxy AND the proxy strips these headers from client
#                requests; otherwise anyone can impersonate anyone.
AUTH_PROVIDER=disabled
# Header names follow the AI Pro guide's gateway contract (the gateway verifies the JWT and passes
# X-User-ID / X-User-Roles downstream). Override if this app sits behind a different gateway.
# X-User-Roles is deliberately not consumed: this app's roles and business-unit scoping are
# application-specific and live in the users table.
AUTH_SSO_EMAIL_HEADER=X-User-Email
AUTH_SSO_USER_HEADER=X-User-ID
AUTH_SSO_NAME_HEADER=X-User-Name
# Comma-separated emails always granted admin and auto-provisioned on first sign-in, so a fresh
# deployment is never locked out. Keep this short and remove entries once real admins exist.
AUTH_BOOTSTRAP_ADMINS=

# AI features. This is the operator-level gate: `disabled` (default) means no LLM call is ever
# made, whatever the in-app AI toggles say. Which features may use AI is configured separately in
# the app's Organization Settings page.
#
#   disabled — AI off entirely (default; app behaves exactly as it did before AI was added)
#   ai_pro   — internal AI Pro gateway. The only provider permitted to carry real applicant data.
#   claude   — public Anthropic API; needs ANTHROPIC_API_KEY
#   openai   — public OpenAI API; needs OPENAI_API_KEY
#
# NOTE: company policy does not permit API access to external enterprise AI. claude and openai are
# retained for reference and offline development against SYNTHETIC data only — they must not be
# selected in any deployment.
LLM_PROVIDER=disabled

# Optional model override. Defaults per provider: aipro-claude-sonnet (ai_pro),
# claude-opus-4-8 (claude), gpt-4o (openai).
# AI Pro models — aipro-standard (6,000 input tokens), aipro-advanced (12,000),
# aipro-claude-sonnet (6,000, optimised for structured output), aipro-vision-lite (multimodal).
LLM_MODEL=

# --- claude provider ---
# Get a key at https://console.anthropic.com → API Keys. This is separate from a claude.ai
# subscription and is billed per use.
ANTHROPIC_API_KEY=

# --- openai provider ---
# Get a key at https://platform.openai.com/api-keys.
OPENAI_API_KEY=
# Only needed to point at a compatible gateway instead of api.openai.com.
OPENAI_BASE_URL=

# --- ai_pro provider (internal) ---
# Service key issued from the AI Platform console (API Key 관리). Bound to a service, expires after
# 90 days. This is the only required setting; the endpoint below has a documented default.
# NOTE: the endpoint is internal-network only — a developer machine needs corporate VPN or the
# ZeroTrust gateway, or every call fails at the network layer.
AI_PRO_API_KEY=
# Override only to point at a different environment. Defaults to https://aipro.samsung.net/v1
AI_PRO_API_URL=
AI_PRO_API_VERSION=v1
# 'openai' (default) or 'anthropic' — which request/response shape to ask AI Pro for.
AI_PRO_MODEL_STYLE=openai

# Justification screening splits applicants across requests to stay inside the model's input
# window. Defaults suit a 6,000-token model; raise them for aipro-advanced (12,000).
AI_SCREENING_BATCH_CHARS=3500
AI_SCREENING_BATCH_SIZE=10

# Confluence 게시는 REST 연동을 만들지 않습니다. 사내 Confluence MCP 서버가 있어서,
# 보고서를 markdown으로 생성한 뒤 Agent가 그 MCP로 게시하는 편이 낫습니다 —
# 코드가 없고, MCP 서버 두 개를 한 Agent가 조합하는 구조가 됩니다. DESIGN.md §9의
# REST 계획은 이 경로로 대체되었습니다.
