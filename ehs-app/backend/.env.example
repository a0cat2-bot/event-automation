PORT=3000
FRONTEND_ORIGIN=http://localhost:5173
DATABASE_URL=postgresql://ehs:ehs@localhost:5432/ehs_app
JWT_COOKIE_NAME=ehs_session
# Local development only. Set a strong, unique secret in every deployed environment.
JWT_SECRET=local-dev-only-change-me

# Required only when using Sally browser-automation features.
SALLY_EMAIL=
SALLY_PASSWORD=

# Email sending. Switch to EMAIL_PROVIDER=knox_portal once the internal integration is ready.
EMAIL_PROVIDER=gmail
GMAIL_USER=
# Gmail App Password (requires Google Account 2FA), not the account login password.
GMAIL_APP_PASSWORD=
EMAIL_FROM_NAME=AX센터 EHS그룹

# Deferred integration (DESIGN.md §9)
CONFLUENCE_API_BASE_URL=
CONFLUENCE_API_TOKEN=
