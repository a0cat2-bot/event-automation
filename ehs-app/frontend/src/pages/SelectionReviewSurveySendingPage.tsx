import { PageShell } from '../components/PageShell';

export function SelectionReviewSurveySendingPage() {
  return (
    <PageShell
      title="Selection Review & Survey Sending"
      description="Final participant review, notification delivery status, survey dispatch, previews, and retries will be managed here."
    />
  );
}
