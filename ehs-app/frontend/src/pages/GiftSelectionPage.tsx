import { PageShell } from '../components/PageShell';

export function GiftSelectionPage() {
  return (
    <PageShell
      title="Gift Recipient Selection"
      description="Eligibility settings, random or manual selection, recipient review, delivery logistics, and notifications will be configured here."
    />
  );
}
