import { PageShell } from '../components/PageShell';

export function ApplicantUploadPage() {
  return (
    <PageShell
      title="Applicant Upload"
      description="CSV selection, mode-specific format guidance, validation preview, conflict resolution, and import confirmation will live here."
    />
  );
}
