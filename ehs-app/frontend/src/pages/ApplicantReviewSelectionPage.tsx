import { PageShell } from '../components/PageShell';

export function ApplicantReviewSelectionPage() {
  return (
    <PageShell
      title="Applicant Review & Selection"
      description="Coordinators will generate a proposed selection, inspect rankings, apply manual overrides, and finalize participants here."
    />
  );
}
