import { PageShell } from '../components/PageShell';

export function ProgramSetupPage() {
  return (
    <PageShell
      title="Program Setup"
      description="Program metadata, selection parameters, templates, and draft or activation actions will be configured here."
    />
  );
}
