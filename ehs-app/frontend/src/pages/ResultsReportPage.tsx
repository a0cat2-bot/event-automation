import { PageShell } from '../components/PageShell';

export function ResultsReportPage() {
  return (
    <PageShell
      title="Results Report & Confluence Draft"
      description="Report preview, export choices, and optional Confluence draft settings and status will live here."
    />
  );
}
