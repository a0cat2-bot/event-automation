import { PageShell } from '../components/PageShell';

export function SurveyResultsPage() {
  return (
    <PageShell
      title="Survey Results Dashboard"
      description="Sally completion metrics, participant status, detailed responses, manual matching, and sync controls will appear here."
    />
  );
}
